void Output (void) {

  // elapsed millis timer reference check
  
 /* for (int i = 0; i < 32; i++) {
    Tlc.set(i, OPvalue[i]);
  }
  
  // create a timer to ensure that outputs are updating using elapsed millis
  // if timer exceeded {
  //  Tlc.clear();
  //  Tlc.update();
  // }
  // else {
      Tlc.update();
  // }    */
}

